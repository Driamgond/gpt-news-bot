
# GPT News Digest - Full Auto System

This project fetches daily English news, summarizes using OpenAI, publishes to GitHub Pages, and sends to MailerLite.

## 🔧 Setup

1. Rename `.env.template` to `.env` and fill in:
    - `OPENAI_API_KEY`
    - `MAILERLITE_API_KEY`
    - `MAILERLITE_GROUP_ID`

2. Push to GitHub and enable GitHub Actions.

3. GitHub Pages will host your blog.
